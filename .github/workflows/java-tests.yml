name: Java Tests

on:
  pull_request:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Download JUnit and Hamcrest
        run: |
          mkdir -p lib
          curl -L -o lib/junit-4.13.2.jar https://search.maven.org/remotecontent?filepath=junit/junit/4.13.2/junit-4.13.2.jar
          curl -L -o lib/hamcrest-core-1.3.jar https://search.maven.org/remotecontent?filepath=org/hamcrest/hamcrest-core/1.3/hamcrest-core-1.3.jar

      - name: Create output directory
        run: mkdir -p out

      - name: Compile main sources
        run: |
          find src/main/java -name "*.java" > sources.txt
          javac -d out @sources.txt

      - name: Compile test sources
        run: |
          find src/test/java -name "*.java" > tests.txt
          javac -cp "lib/*:out" -d out @tests.txt

      - name: Run tests
        run: |
          for test in $(find src/test/java -name "*Test.java"); do
            classname=$(echo $test | sed 's|src/test/java/||' | sed 's|/|.|g' | sed 's|.java$||')
            echo "Running $classname"
            java -cp "lib/*:out" org.junit.runner.JUnitCore "$classname"
          done